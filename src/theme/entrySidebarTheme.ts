import {createTheme, ThemeOptions} from "@mui/material";
import {appTheme} from "x-common-components-app";

export const entrySidebarTheme = createTheme(appTheme as ThemeOptions, {
    components: {
    },
});